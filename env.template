# Copy this file to ".env" in the same folder, then set your school URL.
# 将本文件复制为同目录下的 .env 后填写学校地址（不要把真实 .env 提交到 Git）。

BB_BASE_URL=https://your-blackboard-domain.edu

# Optional: use system-default TLS only (disable campus compatibility adapter)
# BB_TLS_STRICT=1
